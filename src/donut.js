class Donut {
    constructor() {
        this.click = 0;
        this.total = 0;
    }

    getTotalDonuts() {
        return this.total;
    }

    addOne() {
        this.total += 1;
    }



    //    Add one to Count
    // First AutoClicker with 100 count
    // AutoClicker price increases
    // Ensure you can buy AutoClickers
    // AutoClick effects Count when even is called

    // First Multiplier is 10
    // Mutiplier cost goes up
    // You have enough donuts to purchase Multipler
    // First multiplier increase click value 1.2x
    // Sequel multipliers value is exponential to the last
    // Multiplier is sent to AutoClicker


    // Create Click button
    // Create Click count element
    // section for AutoClicks
    // Section for Multiplier
    // Header with dropdown: Co. info, game inspiration, about Developer

    // Click changes donut counter
    // AutoClick and Multiplier unresponsive when not enough
    // Click and Multiplier count displayed
    // Click displays click value
    // Price of bonus buttons displayed

    // Resest the game counts
}
